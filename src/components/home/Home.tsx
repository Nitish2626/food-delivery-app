import { useContext } from 'react';
import NavigationBar from '../navigation bar/NavigationBar';

const Home = () => {

  return (
    <div>
      <NavigationBar />
    </div>
  );
};

export default Home; 